/* Runner acceptance fixture — a PASSING suite. NOT a product suite.
   Exercises the passing-path evidence machinery (archive-before-ledger,
   fatal evidence failures) under fault injection in runner-acceptance.sh. */
console.log('  ok   fixture:always-passes');
console.log('FIXTURE PASS: PASS (1 checks)');
process.exit(0);
